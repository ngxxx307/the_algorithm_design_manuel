get stuck at calculating coordinates.

submit a dfs solution but time limit exceed. Then read the BFS solution and realize that can only use single set because if there are other paths that reach the same node in later round, it is meaningless if there is already a path visited before that is faster.