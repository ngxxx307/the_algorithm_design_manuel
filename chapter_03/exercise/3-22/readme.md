Originally thought to use avl tree with counter. But after asking AI he told me to use two heap approach.

Having trouble of finding the median node. Solved by brainstorm lots of solution, and recognize pattern that left subtree must be all smaller than right subtree and parent node.