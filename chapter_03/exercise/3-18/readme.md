use doubly linked list and update predesscor and successor when insert and delete.

when insert, find the nodes that is closest (closest node must be visited)
when delete, simply rewire the doubly linked list
