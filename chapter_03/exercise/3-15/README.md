Originally thought if i can do it in single pass but found out cannot, because cannot determine the order of the node.

Therefore i decide the traverse the tree to get a sorted arraay first then reconstruct the tree
